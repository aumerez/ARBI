import { EmbeddingGenerationWorker } from './embedding-generation.worker';

describe('EmbeddingGenerationWorker', () => {
  it('should be defined', () => {
    expect(EmbeddingGenerationWorker).toBeDefined();
  });
});
